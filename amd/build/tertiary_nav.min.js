/**
 * Injects a Game Settings option into the quiz tertiary navigation selector.
 *
 * Modelled after local_stackmatheditor/configure.php integration.
 * The option is only added once; duplicate injections are guarded by a
 * data-smg attribute on the <option> element.
 *
 * @module     local_stackmathgame/tertiary_nav
 * @copyright  2026 Ralf Erlebach
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_stackmathgame/tertiary_nav",[],(function(){function inject(config){const select=document.querySelector(".tertiary-navigation .urlselect select");if(!select)return;if(select.querySelector('option[data-smg="quiz"]'))return;const option=document.createElement("option");option.value=config.url,option.textContent=config.label,option.setAttribute("data-smg","quiz"),select.appendChild(option)}return{init:function(config){"loading"===document.readyState?document.addEventListener("DOMContentLoaded",(function(){inject(config)})):inject(config)}}}));

//# sourceMappingURL=tertiary_nav.min.js.map