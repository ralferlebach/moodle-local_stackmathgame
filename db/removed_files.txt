# Files removed in earlier releases of local_stackmathgame.
#
# Unpacking a plugin ZIP adds and overwrites files but never deletes any, so a file removed
# upstream survives in an installed copy or in a repository updated that way. It then still
# autoloads, duplicates a class, or reads columns that no longer exist. The `stale-files` CI
# job turns that into a loud failure instead of confusing downstream errors.
#
# One path per line, relative to the plugin root.

.github/workflows/moodle-ci.yml
amd/src/fantasy_quiz.js
amd/build/fantasy_quiz.min.js
amd/build/fantasy_quiz.min.js.map

# A Behat context copied in from local_stackmatheditor, a different plugin. Its class was
# behat_local_stackmatheditor, it declared @package local_stackmatheditor, and its steps drove a
# MathQuill editor this plugin does not ship - set_config() calls even targeted the other
# component. No feature file in this plugin referenced any of its fifteen steps.
tests/behat/behat_local_stackmatheditor.php
