##############################################################################
# Moodle Plugin CI - main (release) pipeline - local_stackmathgame
#
# Runs ONLY on push / PR to main; provides the "CI complete" status check for
# branch protection. Full SEQUENTIAL pipeline per matrix cell across the
# supported Moodle 4.5/5.0/5.2 x PHP 8.2-8.4 x DB matrix. PHP 8.4 is deliberately absent
# for Moodle 4.5: 4.5 does not support it, so those cells would fail on core rather than on
# this plugin. The plugin ships AMD modules in
# amd/src/ and in mode/*/amd/src/, so grunt + mustache run as well. The fast parallel pipeline lives in moodle-plugin-ci-dev.yml.
##############################################################################

name: Moodle Plugin CI Main

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency:
  group: ci-main-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: "CI / ${{ matrix.moodle }} / PHP ${{ matrix.php }} / ${{ matrix.database }}"
    runs-on: ubuntu-22.04
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: 'postgres'
          POSTGRES_HOST_AUTH_METHOD: 'trust'
        ports:
          - 5432:5432
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 3
      mariadb:
        image: mariadb:10.11
        env:
          MYSQL_USER: 'root'
          MYSQL_ALLOW_EMPTY_PASSWORD: "true"
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval 10s --health-timeout 5s --health-retries 3
    strategy:
      fail-fast: false
      matrix:
        include:
          - { moodle: MOODLE_405_STABLE, php: '8.2', database: pgsql }
          - { moodle: MOODLE_405_STABLE, php: '8.2', database: mariadb }
          - { moodle: MOODLE_405_STABLE, php: '8.3', database: pgsql }
          - { moodle: MOODLE_405_STABLE, php: '8.3', database: mariadb }
          - { moodle: MOODLE_500_STABLE, php: '8.2', database: pgsql }
          - { moodle: MOODLE_500_STABLE, php: '8.3', database: mariadb }
          - { moodle: MOODLE_500_STABLE, php: '8.4', database: pgsql }
          - { moodle: MOODLE_502_STABLE, php: '8.3', database: pgsql }
          - { moodle: MOODLE_502_STABLE, php: '8.3', database: mariadb }
          - { moodle: MOODLE_502_STABLE, php: '8.4', database: pgsql }
          - { moodle: MOODLE_502_STABLE, php: '8.4', database: mariadb }
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
        with:
          path: plugin
      - name: Prepare log directory
        run: mkdir -p ci-logs
      - name: Setup PHP ${{ matrix.php }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          ini-values: max_input_vars=5000
          coverage: none
      - name: Cache Composer downloads
        uses: actions/cache@v4
        with:
          path: ~/.cache/composer
          key: composer-${{ runner.os }}-${{ github.sha }}
          restore-keys: composer-${{ runner.os }}-
      - name: Initialise moodle-plugin-ci
        run: |
          composer create-project -n --no-dev --prefer-dist moodlehq/moodle-plugin-ci ci ^4
          echo $(cd ci/bin; pwd) >> $GITHUB_PATH
          echo $(cd ci/vendor/bin; pwd) >> $GITHUB_PATH
          sudo locale-gen en_AU.UTF-8
      - name: Install Maxima and gnuplot
        run: |
          # qtype_stack's install.php runs a live CAS healthcheck while the site is being
          # installed. Without a maxima binary it throws a coding exception, install_database.php
          # aborts, and moodle-plugin-ci never gets to write MOODLE_DIR into the job environment.
          # Every later step then resolves the Moodle root to the workspace and fails with a
          # message about something else entirely: "Unable to find Gruntfile",
          # "Could not load core_component", "vendor/bin/phpunit: not found".
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends maxima maxima-share gnuplot-nox
          maxima --version

      - name: Install Moodle, dependencies and plugin
        run: |
          # Hard dependencies from version.php: qtype_stack, qbehaviour_stackmathgame,
          # filter_shortcodes. qbehaviour_adaptivemultipart is a hard dependency OF
          # qtype_stack -- without it the Moodle install aborts before our plugin is
          # ever reached. block_xp / block_stash are soft integrations; they are
          # installed so the optional bridges are exercised rather than skipped.
          moodle-plugin-ci add-plugin --branch master maths/moodle-qtype_stack
          moodle-plugin-ci add-plugin --branch master maths/moodle-qbehaviour_adaptivemultipart
          moodle-plugin-ci add-plugin --branch main ralferlebach/moodle-qbehaviour_stackmathgame
          moodle-plugin-ci add-plugin --branch master branchup/moodle-filter_shortcodes
          moodle-plugin-ci add-plugin --branch master FMCorz/moodle-block_xp
          moodle-plugin-ci add-plugin --branch master FMCorz/moodle-block_stash
          moodle-plugin-ci install --plugin ./plugin --db-host=127.0.0.1
        env:
          DB: ${{ matrix.database }}
          MOODLE_BRANCH: ${{ matrix.moodle }}
      - name: PHP Lint
        if: ${{ always() }}
        run: set -o pipefail; moodle-plugin-ci phplint moodle/local/stackmathgame 2>&1 | tee ci-logs/phplint.log
      - name: PHP Copy/Paste Detector
        continue-on-error: true
        if: ${{ always() }}
        run: set -o pipefail; moodle-plugin-ci phpcpd moodle/local/stackmathgame 2>&1 | tee ci-logs/phpcpd.log
      - name: PHP Mess Detector
        continue-on-error: true
        if: ${{ always() }}
        run: set -o pipefail; moodle-plugin-ci phpmd moodle/local/stackmathgame 2>&1 | tee ci-logs/phpmd.log
      - name: Moodle Code Checker
        if: ${{ always() }}
        run: set -o pipefail; moodle-plugin-ci codechecker moodle/local/stackmathgame --max-warnings 0 --exclude=PSR1.Classes.ClassDeclaration,moodle.Commenting.TodoComment 2>&1 | tee ci-logs/codechecker.log
      - name: Moodle PHPDoc Checker
        if: ${{ always() }}
        run: set -o pipefail; moodle-plugin-ci phpdoc moodle/local/stackmathgame --max-warnings 0 2>&1 | tee ci-logs/phpdoc.log
      - name: Validating
        if: ${{ always() }}
        run: set -o pipefail; moodle-plugin-ci validate moodle/local/stackmathgame 2>&1 | tee ci-logs/validate.log
      - name: Check upgrade savepoints
        if: ${{ always() }}
        run: set -o pipefail; moodle-plugin-ci savepoints moodle/local/stackmathgame 2>&1 | tee ci-logs/savepoints.log
      - name: Mustache Lint
        if: ${{ always() }}
        run: set -o pipefail; moodle-plugin-ci mustache moodle/local/stackmathgame 2>&1 | tee ci-logs/mustache.log
      - name: Grunt (AMD build + ESLint)
        if: ${{ always() }}
        # local_stackmathgame ships AMD modules in amd/src/ AND in every mode subplugin
        # (mode/*/amd/src/). --max-lint-warnings 0 keeps ESLint warnings failing, which is
        # what caught the undocumented @returns in the mode modules.
        run: set -o pipefail; moodle-plugin-ci grunt moodle/local/stackmathgame --max-lint-warnings 0 2>&1 | tee ci-logs/grunt.log
      - name: Jest (AMD unit tests)
        if: ${{ always() }}
        run: |
          set -o pipefail
          cd plugin/tests/jest
          npm install --no-audit --no-fund
          npx jest 2>&1 | tee ../../../ci-logs/jest.log
      - name: PHPUnit tests
        if: ${{ always() }}
        run: set -o pipefail; moodle-plugin-ci phpunit moodle/local/stackmathgame --fail-on-warning --testdox 2>&1 | tee ci-logs/phpunit.log
      - name: Behat features
        id: behat
        if: ${{ always() }}
        run: set -o pipefail; moodle-plugin-ci behat moodle/local/stackmathgame --profile chrome --tags "@local_stackmathgame&&~@broken" --dump 2>&1 | tee ci-logs/behat.log
      - name: Collect error summary
        if: ${{ always() }}
        run: |
          if [ -d "$GITHUB_WORKSPACE/moodledata/behat_dump" ]; then
            mkdir -p ci-logs/behat_dump
            cp -a "$GITHUB_WORKSPACE/moodledata/behat_dump/." ci-logs/behat_dump/
          fi
          {
            echo "job: ${{ github.job }} (${{ matrix.moodle }} / PHP ${{ matrix.php }} / ${{ matrix.database }})"
            echo "status: ${{ job.status }}"
            echo "ref:    ${{ github.ref }}"
            echo ""
            ls -1 ci-logs
          } > ci-logs/summary.txt
          cat ci-logs/summary.txt
      - name: Upload error summary (ZIP)
        if: ${{ always() }}
        uses: actions/upload-artifact@v7
        with:
          name: error-summary-main-${{ matrix.moodle }}-${{ matrix.php }}-${{ matrix.database }}
          path: ci-logs
          retention-days: 7
          if-no-files-found: ignore
      - name: Upload Behat screenshots (ZIP)
        if: ${{ failure() && steps.behat.outcome == 'failure' }}
        uses: actions/upload-artifact@v7
        with:
          name: behat-screenshots-main-${{ matrix.moodle }}-${{ matrix.php }}-${{ matrix.database }}
          path: ${{ github.workspace }}/moodledata/behat_dump
          retention-days: 7
          if-no-files-found: warn

  ecosystem-lockstep:
    name: Ecosystem version lockstep
    runs-on: ubuntu-22.04
    steps:
      - name: Check out plugin
        uses: actions/checkout@v4
        with:
          path: plugin

      - name: Bundled mode subplugins and qbehaviour_stackmathgame must be in lockstep
        run: |
          get_version() {
            grep -oE '\$plugin->version[[:space:]]*=[[:space:]]*[0-9]+' "$1" \
              | grep -oE '[0-9]+$'
          }
          own=$(get_version plugin/version.php)
          echo "local_stackmathgame: $own"
          rc=0

          # 1. The three bundled mode subplugins live in THIS repository. They are shipped in
          #    the same ZIP, so a version drift between them and the parent is never a rollout
          #    artefact -- it is always a mistake, and it is enforced unconditionally.
          for mode in plugin/mode/*/version.php; do
            [ -e "$mode" ] || continue
            modename=$(basename "$(dirname "$mode")")
            modever=$(get_version "$mode")
            echo "  stackmathgamemode_${modename}: $modever"
            if [[ "$modever" -gt "$own" ]]; then
              echo "  FEHLER: Ein Subplugin darf nie neuer sein als das Elternplugin."
              rc=1
            fi
          done
          if [[ $rc -ne 0 ]]; then
            echo "Die gebuendelten Mode-Subplugins sind nicht konsistent versioniert."
            exit 1
          fi

          # 2. qbehaviour_stackmathgame is a SEPARATE repository and a hard dependency. Two
          #    repositories can only be pushed one after another, so during a rollout the sibling
          #    is legitimately still on the previous version. Failing here would make the first
          #    push red every single time -- the gate would be noise, not a gate. It is therefore
          #    enforced where "released together" is actually claimed: on a tag.
          if git clone --depth 1 --branch main \
              "https://github.com/ralferlebach/moodle-qbehaviour_stackmathgame.git" \
              /tmp/qbehaviour >/dev/null 2>&1; then
            sibver=$(get_version /tmp/qbehaviour/version.php)
            echo "  qbehaviour_stackmathgame: $sibver"
            if [[ "$sibver" != "$own" ]]; then
              if [[ "${{ github.ref_type }}" == "tag" ]]; then
                echo "Release gate: ein Tag verlangt local_stackmathgame und"
                echo "qbehaviour_stackmathgame auf derselben Version."
                exit 1
              fi
              echo "::warning::Versionen weichen ab - waehrend eines Rollouts erwartet."
              echo "Kein Fehler: auf einem Branch-Push wird der Gleichstand nur berichtet."
            fi
          else
            echo "::warning::qbehaviour_stackmathgame konnte nicht geklont werden."
          fi
          echo "Ecosystem-Check abgeschlossen."

  release-artefact:
    name: Release artefact contents
    runs-on: ubuntu-22.04
    steps:
      - name: Check out plugin
        uses: actions/checkout@v4

      - name: Build the release archive exactly as it will be distributed
        run: |
          # git archive is what honours .gitattributes export-ignore. Building the artefact here
          # means the gate tests the file set that is actually shipped, not the working tree.
          git archive --format=tar --prefix=stackmathgame/ HEAD > /tmp/release.tar
          tar -tf /tmp/release.tar > /tmp/release.list
          echo "Artefakt enthält $(wc -l < /tmp/release.list) Einträge."

      - name: No developer tooling, CI plumbing or test harnesses may ship
        run: |
          rc=0
          while read -r forbidden; do
            if grep -qE "^stackmathgame/${forbidden}" /tmp/release.list; then
              echo "VERBOTEN im Release-Artefakt: $forbidden"
              grep -E "^stackmathgame/${forbidden}" /tmp/release.list | head -5
              rc=1
            fi
          done <<'PATTERNS'
          tools/
          docs/
          \.github/
          tests/load/
          tests/playwright/
          \.gitattributes
          \.gitignore
          makefile
          phpcs\.xml
          PATTERNS
          if [[ $rc -ne 0 ]]; then
            echo "Das Release-Artefakt darf keine Entwicklerwerkzeuge enthalten."
            exit 1
          fi
          echo "Artefakt ist sauber."

      - name: The artefact must still contain what Moodle runs
        run: |
          for required in stackmathgame/version.php stackmathgame/lang stackmathgame/db; do
            if ! grep -q "^${required}" /tmp/release.list; then
              echo "FEHLT im Release-Artefakt: $required"
              exit 1
            fi
          done
          echo "Pflichtinhalte vorhanden."

  coverage:
    name: Coverage
    runs-on: ubuntu-22.04
    services:
      postgres:
        image: postgres@sha256:6efd0df010dc3cb40d5e33e3ef84acecc5e73161bd3df06029ee8698e5e12c60
        env:
          POSTGRES_USER: 'postgres'
          POSTGRES_HOST_AUTH_METHOD: 'trust'
        ports:
          - 5432:5432
        options: --health-cmd=pg_isready --health-interval=10s --health-timeout=5s --health-retries=3
    steps:
      - name: Check out plugin
        uses: actions/checkout@v4
        with:
          path: plugin

      - name: Set up PHP with a coverage driver
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pgsql, zip, gd, xmlrpc, soap, intl
          # pcov is enough for line coverage and far faster than xdebug.
          coverage: pcov
          ini-values: max_input_vars=5000

      - name: Initialise moodle-plugin-ci
        run: |
          composer create-project -n --no-dev --prefer-dist             moodlehq/moodle-plugin-ci ci ^4
          echo $(cd ci/bin; pwd) >> $GITHUB_PATH
          echo $(cd ci/vendor/bin; pwd) >> $GITHUB_PATH
          sudo locale-gen en_AU.UTF-8
          echo "NVM_DIR=$HOME/.nvm" >> $GITHUB_ENV

      - name: Install Maxima and gnuplot
        run: |
          # qtype_stack's install.php runs a live CAS healthcheck during site installation.
          # Without a maxima binary it aborts the whole install.
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends maxima maxima-share gnuplot-nox
          maxima --version

      - name: Install Moodle and the plugin ecosystem
        run: |
          # Hard dependencies from version.php: qtype_stack, qbehaviour_stackmathgame,
          # filter_shortcodes. qbehaviour_adaptivemultipart is a hard dependency OF
          # qtype_stack -- without it the Moodle install aborts before our plugin is
          # ever reached. block_xp / block_stash are soft integrations; they are
          # installed so the optional bridges are exercised rather than skipped.
          moodle-plugin-ci add-plugin --branch master maths/moodle-qtype_stack
          moodle-plugin-ci add-plugin --branch master maths/moodle-qbehaviour_adaptivemultipart
          moodle-plugin-ci add-plugin --branch main ralferlebach/moodle-qbehaviour_stackmathgame
          moodle-plugin-ci add-plugin --branch master branchup/moodle-filter_shortcodes
          moodle-plugin-ci add-plugin --branch master FMCorz/moodle-block_xp
          moodle-plugin-ci add-plugin --branch master FMCorz/moodle-block_stash
          moodle-plugin-ci install --plugin ./plugin --db-host=127.0.0.1
        env:
          DB: pgsql
          MOODLE_BRANCH: MOODLE_405_STABLE

      - name: Run PHPUnit with coverage
        run: |
          # Clover XML instead of --coverage-text: the text report covers the whole Moodle tree
          # (its summary showed ~2% because it counts all of core), which says nothing about this
          # plugin. Clover gives per-file counters, so the gate can measure exactly this plugin.
          moodle-plugin-ci phpunit --coverage-clover --coverage-pcov moodle/local/stackmathgame
        env:
          DB: pgsql
          MOODLE_BRANCH: MOODLE_405_STABLE

      - name: Enforce the coverage floor for this plugin
        run: |
          report=$(find . -name 'coverage.xml' -o -name 'clover.xml' | head -1)
          if [[ -z "$report" ]]; then
            echo "Kein Clover-Report gefunden - das Gate kann nicht bewertet werden."
            exit 1
          fi
          echo "Report: $report"
          # The gate measures THIS plugin only. Counting every path containing 'stackmathgame' would
          # also include the three sibling plugins that are installed alongside but whose tests do
          # not run here - that produced a meaningless 0.5% and made the gate useless.
          php plugin/tools/coverage_gate.php "$report" "$COVERAGE_FLOOR"
        env:
          COVERAGE_FLOOR: '50'

  maturity-gate:
    name: Release gate
    needs: [test, ecosystem-lockstep, release-artefact, coverage]
    runs-on: ubuntu-22.04
    # Without always() this job is skipped whenever one of its dependencies fails - and a skipped
    # job is not re-executed by "re-run failed jobs", so it stays skipped for good and keeps
    # ci-complete red even after every real failure has been fixed.
    if: always()
    steps:
      - name: Check out plugin
        uses: actions/checkout@v4

      - name: Every release gate must be green
        run: |
          # This job is the single place where the release gates are judged, so it reports each
          # result and fails on any that is not green - whatever the declared maturity is.
          rc=0
          for entry in             "test=${{ needs.test.result }}"             "ecosystem-lockstep=${{ needs.ecosystem-lockstep.result }}"             "release-artefact=${{ needs.release-artefact.result }}"             "coverage=${{ needs.coverage.result }}"; do
            echo "  ${entry%%=*}: ${entry#*=}"
            if [[ "${entry#*=}" != "success" ]]; then
              rc=1
            fi
          done
          if [[ $rc -ne 0 ]]; then
            echo "Mindestens ein Release-Gate ist nicht gruen (siehe Liste oben)."
            exit 1
          fi
          maturity=$(grep -oE 'MATURITY_[A-Z]+' version.php | head -1)
          echo "Deklarierter Reifegrad: ${maturity:-unbekannt}"
          echo "Alle Release-Gates sind gruen."

  stale-files:
    name: Stale files
    runs-on: ubuntu-22.04
    steps:
      - name: Check out plugin
        uses: actions/checkout@v4

      - name: Files removed in earlier releases must not still be present
        run: |
          # Unpacking a plugin ZIP adds and overwrites files but never deletes any, so a file
          # removed upstream survives in an installed copy or in a repository updated that way.
          # It then still autoloads, duplicates a class, or reads columns that no longer exist.
          # This job turns that into a loud failure instead of confusing downstream errors.
          list=db/removed_files.txt
          if [[ ! -f "$list" ]]; then
            echo "Keine Liste entfernter Dateien vorhanden - nichts zu pruefen."
            exit 0
          fi
          rc=0
          while IFS= read -r path; do
            [[ -z "$path" || "$path" == \#* ]] && continue
            if [[ -e "$path" ]]; then
              echo "Veraltete Datei ist noch vorhanden: $path"
              rc=1
            fi
          done < "$list"
          if [[ $rc -ne 0 ]]; then
            echo "Diese Dateien wurden in frueheren Releases entfernt und muessen geloescht werden."
            exit 1
          fi
          echo "Keine veralteten Dateien gefunden."

  ci-complete:
    name: CI complete
    needs: [maturity-gate, stale-files]
    runs-on: ubuntu-22.04
    if: always()
    steps:
      - name: All matrix jobs must have succeeded
        run: |
          # Every gate is named individually. The previous message printed only two of them, so a
          # failure in any other job looked like an unexplained red build.
          rc=0
          # Only the two aggregating jobs are evaluated here. The matrix, lockstep, artefact and
          # coverage results are judged once, inside maturity-gate; checking them here as well
          # duplicated the same condition in two places.
          for entry in             "maturity-gate=${{ needs.maturity-gate.result }}"             "stale-files=${{ needs.stale-files.result }}"; do
            name="${entry%%=*}"
            result="${entry#*=}"
            echo "  $name: $result"
            if [[ "$result" != "success" ]]; then
              rc=1
            fi
          done
          if [[ $rc -ne 0 ]]; then
            echo "Mindestens ein Gate ist nicht gruen (siehe Liste oben)."
            exit 1
          fi
          echo "All CI gates passed."
